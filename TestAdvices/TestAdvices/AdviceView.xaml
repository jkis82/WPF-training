<Window x:Class="TestAdvices.AdviceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestAdvices"
        xmlns:viewmodel="clr-namespace:TestAdvices.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
   <Window.DataContext>
      <viewmodel:AdviceViewModel></viewmodel:AdviceViewModel>
   </Window.DataContext>
    <Grid>
       <Grid.RowDefinitions>
         <RowDefinition Height="Auto" />
         <RowDefinition Height="*" />
      </Grid.RowDefinitions>
       <ListView x:Name="AdviceListView" Margin="5" ItemsSource="{Binding VmAdvices}">
          <ListView.ItemTemplate>
             <DataTemplate>
                <StackPanel Orientation="Horizontal">
                  <TextBlock Margin="5" Text="{Binding State}" />
                  <TextBlock Margin="5" Text="{Binding Advice.Id}" />
                  <TextBlock Margin="5" Text="{Binding Advice.Name}" />
                  <TextBlock Margin="5" Text="{Binding Advice.IsActive}" />
               </StackPanel>
             </DataTemplate>
          </ListView.ItemTemplate>
       </ListView>
       <Button Grid.Row="1" HorizontalAlignment="Center" Height="20" Width="120">Ok</Button>
    </Grid>
</Window>
